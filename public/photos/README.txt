Replace these placeholder SVGs with your own photos.

Option 1: keep the filenames and overwrite the files with matching .svg files.
Option 2 (recommended): add JPG/PNG/WebP files and update src/config.ts.

Example:
  image: "/photos/blake.jpg"
  image: "/photos/chase.jpg"
  revealPhoto: "/photos/us.jpg"
